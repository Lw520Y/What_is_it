# 修改记录

## 2026-09-24 修复：滑动查看文件夹大小时偶发闪退

**涉及文件**：`main.py`、`scanner.py`（2 个文件，+191 / -98 行）

### 问题描述

鼠标滑动（连续选中）文件夹条目查看大小时，软件偶发闪退；算大小/扫描进行中刷新、切换盘符或关闭窗口也可能触发。

### 原因分析（已离屏脚本复现确认）

1. **跨线程携带树节点导致悬空引用**：原实现每次展开目录都创建 `ScanWorker`（QThread），把 `QTreeWidgetItem` 对象直接传进后台线程再随信号发回主线程。滑动过程中一旦刷新/切换盘符，`tree.clear()` 会销毁这些节点，迟到的扫描信号仍在向已销毁的对象写入（复现日志：`RuntimeError: Internal C++ object already deleted`）。
2. **QThread 生命周期失控触发 qFatal（闪退元凶）**：每选中一个文件夹就新建一个 `QThread`，滑动一下会瞬间创建几十个线程；窗口销毁时只要有线程仍在运行，Qt 会以 `qFatal` 直接杀死进程（复现日志：`QThread: Destroyed while thread '' is still running`）。此外线程清理回调在工作线程执行、与主线程的引用列表操作存在竞态，可能丢失线程引用。

### 修复方案（main.py）

- **常驻后台服务线程取代临时 QThread**：新增 `_Service`（扫描/AI 单线程，大小计算 3 线程小线程池）、`_Task`、`_Relay`（跨线程结果信号中转）、`_Gate`（代际号）；删除 `ScanWorker` / `SizeWorker` / `AiWorker` 及 `_threads` 引用列表。
- **后台线程不再触碰任何 Qt 对象**：任务只携带路径、代际号等纯数据；结果回到主线程后按路径在当前树中重新查找条目，找不到即安全丢弃。
- **代际号作废旧结果**：每次清空树（刷新/切换盘符）代际号自增，所有在途任务的迟到结果一律作废，杜绝悬空节点写入。
- **规则识别移入后台线程**：`rules.identify` 的子目录探测读盘不再卡住界面，大目录扫描时 UI 更流畅。
- **关窗安全退出**：新增 `closeEvent`，关窗时作废所有在途任务、协作式取消并等待线程退出，消除 qFatal 闪退。
- **滑动多选体验增强**：按住滑动划过多个文件夹时，现在每个被划过的文件夹都会排队计算大小（原先只有第一个选中项会计算）。

### 修复方案（scanner.py）

- `scan_dir` / `calculate_dir_size` 增加可选的 `cancel`（`threading.Event`）参数，在遍历循环中逐项检查取消标志，可被快速中止；返回值增加 `cancelled` 字段。

### 验证情况

- 竞态回归脚本（模拟滑动选中 + 期间清树 + 算大小进行中直接关窗）连续多轮通过：无 `RuntimeError`、无 qFatal，退出码 0。
- 功能回归：滑动选中的每个文件夹均正常显示大小；展开加载子级、详情面板（大小/文件数/子目录数）正常。
- 真实盘符冒烟（源码 + 打包 exe）：扫描真实盘根目录 → 滑动选中多个目录 → 计算中途切换盘符 → 关窗，均干净退出（exe 约 2 秒内退出），无进程残留、无内存异常增长。
- 已重新打包 `dist\WhatIsIt.exe`（含本修复）。

### 建议的提交信息

```
修复滑动查看文件夹大小时偶发闪退

- 根因一：扫描线程跨线程携带 QTreeWidgetItem，树被清空后产生悬空引用
- 根因二：每次选中/展开都新建 QThread，销毁时仍有线程在跑触发 qFatal
- 改为常驻后台服务线程池，任务只携带路径等纯数据，结果经信号回主线程
  后按路径重新查找条目；引入代际号作废刷新/切盘后的迟到结果
- scanner 扫描/大小计算支持协作式取消；关窗时统一取消并等待线程退出
- 规则识别移入后台线程避免卡界面；滑动多选时逐个排队计算大小
```
